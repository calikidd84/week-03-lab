## Week 3 Reflection

1. What was the most confusing thing about Python compared to JavaScript?
   - Nothing, I have experience in python before
2. What does an HTTP status code tell you? Give one example.
   - Tells you whether the request was successfully or not to the server/backend. Example: an 200 is success, or 400 not successful!
3. What was the difference between a path parameter and a query parameter?
   - Path parameters identify a specific resource, while query parameters sort, filter, or modify that resource
4. What would happen to all the data if you restarted the server right now? 
   Why is that a problem, and what will we use to fix it?

   - If restarted, the data that was just entered is gone. This is problem with real world apps because normally we want to keep the data or for data to persist. To fix this, we will a database in the future!